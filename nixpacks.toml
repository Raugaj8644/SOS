[phases.install]
cmds = [
  "printf 'packages:\\n  - packages/shared\\n  - apps/api\\n' > pnpm-workspace.yaml",
  "pnpm install --no-frozen-lockfile"
]

[phases.build]
cmds = ["pnpm --filter @cerp/shared build && pnpm --filter @cerp/api build"]

[start]
cmd = "node apps/api/dist/main.js"
