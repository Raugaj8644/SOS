[phases.setup]
aptPkgs = ["curl"]

[phases.build]
cmds = ["pnpm --filter @cerp/shared build && pnpm --filter @cerp/api build && ls -la /app/apps/api/dist/"]

[start]
cmd = "node apps/api/dist/main.js"
