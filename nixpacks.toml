[phases.install]
cmds = ["pnpm install --filter @cerp/shared --filter @cerp/api --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @cerp/shared build && pnpm --filter @cerp/api build"]

[start]
cmd = "node apps/api/dist/main.js"
